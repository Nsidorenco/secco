x := input();
y := input();

% preconditions %
if y > 2 then error() else y := 0;
if x > 0 then error() else x := 0;

while x < 4 do
    if y > 2 then 
      x := x - 1
    else 
      x := x + 1
end
